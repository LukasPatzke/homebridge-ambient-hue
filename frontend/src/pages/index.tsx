import { Center } from '@mantine/core';
import { FC } from 'react';
import { ReactComponent as AppIconColor } from '../assets/icon-color.svg';

export const RootIndexPage: FC = () => (
  <Center>
    <AppIconColor height={400} />
  </Center>
);
